<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher_custom"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_custom"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarefastindoAbsensi"
        tools:targetApi="31">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_GOOGLE_MAPS_API_KEY_HERE" />

        <!-- Splash Screen Launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.CarefastindoAbsensi.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Screen -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.CarefastindoAbsensi.NoActionBar"
            android:windowSoftInputMode="adjustResize" />
            
        <!-- Forgot Password Screen -->
        <activity
            android:name=".ui.login.ForgotPasswordActivity"
            android:exported="false"
            android:theme="@style/Theme.CarefastindoAbsensi.NoActionBar"
            android:windowSoftInputMode="adjustResize" />

        <!-- Employee Main Workspace -->
        <activity
            android:name=".ui.employee.EmployeeMainActivity"
            android:exported="false"
            android:theme="@style/Theme.CarefastindoAbsensi.NoActionBar" />

        <!-- Admin Main Workspace -->
        <activity
            android:name=".ui.admin.AdminMainActivity"
            android:exported="false"
            android:theme="@style/Theme.CarefastindoAbsensi.NoActionBar" />

        <!-- Tentang Aplikasi Screen -->
        <activity
            android:name=".ui.about.TentangAplikasiActivity"
            android:exported="false"
            android:theme="@style/Theme.CarefastindoAbsensi.NoActionBar" />

    </application>

</manifest>
